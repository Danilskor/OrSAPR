namespace BottleParameters
{
    /// <summary>
    /// Contains parameter type
    /// </summary>
    public enum ParameterTypeEnum
    {
        CoverRadius,
        HandleBaseRadius,
        HandleRadius,
        HandleLength,
        Height,
        Width,
        WallThickness
    }
}
